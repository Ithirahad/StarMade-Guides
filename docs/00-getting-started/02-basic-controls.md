# Basic Controls

All key bindings can be remapped in the game settings. The defaults listed here are the standard layout.

## General

| Action | Default Key |
|--------|-------------|
| Chat | `Enter` |
| Select / look at entity | `F` |
| Open inventory | `I` |
| Open navigation panel | `N` |
| Open faction menu | `H` |

## Astronaut Mode

| Action | Default Key |
|--------|-------------|
| Move forward | `W` |
| Move backward | `S` |
| Strafe left | `A` |
| Strafe right | `D` |
| Move (slow) | `Left Shift` |
| Look around | Mouse |
| Activate / interact | `R` |
| Select item in hotbar | `1`–`9`, `0` / Mouse wheel |
| Use / fire selected item | `Left Mouse Button` |
| Secondary use | `Right Mouse Button` |
| Sit | `O` |

## Astronaut Mode — In Gravity

| Action | Default Key |
|--------|-------------|
| Jump | `Space` |

## Astronaut Mode — Zero Gravity

In zero-G all the same movement keys apply, plus:

| Action | Default Key |
|--------|-------------|
| Float up | `E` |
| Float down | `Q` |
| Align to nearby structure | `Space` (tap while near) |

> **Tip:** Aligning to a structure also causes you to move with it if the structure is flying. This is useful for boarding moving ships.

## Ship — Flight Mode

| Action | Default Key |
|--------|-------------|
| Accelerate forward | `W` |
| Decelerate / reverse | `S` |
| Strafe up | `Space` |
| Strafe down | `Left Ctrl` |
| Strafe left | `A` |
| Strafe right | `D` |
| Roll left / right | `Q` / `E` |
| Aim / look | Mouse |
| Fire selected weapon | `Left Mouse Button` |
| Select weapon slot | `1`–`9`, `0` / Mouse wheel |
| Toggle build / flight mode | `Z` |
| Exit ship | `R` |
| Open weapon panel | `G` |
| Open structure panel | `Delete` |
| Select target | `F` |
| Set current target as turret/AI target | `X` |

## Ship — Build Mode

| Action | Default Key |
|--------|-------------|
| Move (fast) | `W` / `A` / `S` / `D` (+ `Left Shift`) |
| Move up / down | `Space` / `Left Ctrl` |
| Place block | `Left Mouse Button` |
| Remove block | `Right Mouse Button` |
| Select block in hotbar | `1`–`9`, `0` / Mouse wheel |
| Advanced Build Tools | `Left Ctrl` (hold) |
| Build Shapes/Modes radial menu | `` ` `` |
| Select module (for linking) | `C` |
| Connect module | `V` |
| Undo last action | `Ctrl+Z` |
| Toggle flight / build mode | `Z` |
| Open catalog | `U` |
| Spawn new ship | `X` |

## Block Interaction

| Action | Default Key |
|--------|-------------|
| Activate a block | `R` |
| Select module for connection | `C` |
| Connect to selected module | `V` |

## Quick Reference Card

